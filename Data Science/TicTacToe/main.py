from game_messages import welcome_message
import copy
import game
import constants
from board import board as boardClass
from minimax import *
from minimax_aplha_beta import get_best_move_maximizer as get_move_with_alpha_beta

# welcome_message()

game.init_game()

# board = boardClass()
# board.make_move(0, 2)
# board.make_move(1, 2)

# board.toggle_turn()

# board.make_move(0, 0)
# board.make_move(2, 2)

# board.toggle_turn()
# board.print_board()

# newBoard = copy.deepcopy(board)

# move = get_move_with_alpha_beta(newBoard)
# print(move)

# board.make_move(move[0], move[1])
# board.print_board()


